checks:
  large:
    parameters:
      max-lines: -1
  stale:
    activation: off
    parameters:
      max-idle-days: -1
  long-lived:
    activation: off
    parameters:
      max-days: -1

workflows:
  - name: test
    run: $addLabel("stale")
