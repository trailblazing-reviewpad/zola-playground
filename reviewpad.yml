checks:
  large:
    parameters:
      max-lines: -1
  stale:
    parameters:
      max-idle-days: -1
  long-lived:
    activation: off
    parameters:
      max-days: -1
  long-pick-up-time:
    activation: off
    parameters:
      max-hours: -1

workflows:
  - name: test
    run: $addLabel("stale")
