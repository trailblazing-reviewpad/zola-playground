checks:
  non-conventional-commits:
    activation: on
  stale:
    parameters:
      max-idle-days: -1
  large:
    parameters:
      max-lines: -1
  high-code-churn:
    parameters:
      max-rate: -1
      min-deleted-lines: -1
     
