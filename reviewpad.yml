checks:
  large:
    parameters:
      max-lines: -1
  stale:
    activation: on
    parameters:
      max-idle-days: -1
  long-lived:
    activation: on
    parameters:
      max-days: -1

workflows:
  - name: test
    run: $addLabel("stale")
